% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{crps_normal}
\alias{crps_normal}
\title{Negated CRPS for normal predictive distributions}
\usage{
crps_normal(mu, sigma, x)
}
\arguments{
\item{mu}{Numeric vector. Location parameters (conditional means).}

\item{sigma}{Numeric vector. Scale parameters (conditional SDs, > 0).}

\item{x}{Numeric vector. Realised observations.}
}
\value{
Numeric vector of CRPS values in \verb{(-Inf, 0]} (negated loss).
}
\description{
Computes the Continuous Ranked Probability Score for a normal predictive
distribution using \code{scoringRules::crps_norm()} and negates it so that higher
values are better.
}
\details{
Calls \code{scoringRules::crps_norm(y = x, mean = mu, sd = sigma)} and
negates. Use for GARCH(1,1)-norm forecasts where mu is the conditional
mean and sigma is the conditional standard deviation.
}
\examples{
if (requireNamespace("scoringRules", quietly = TRUE)) {
  crps_normal(mu = c(0, 1), sigma = c(1, 2), x = c(0.2, 1.3))
}

}
