% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictable_bounds.R
\name{predictable_rejections}
\alias{predictable_rejections}
\title{Summarise predictable bounds e-process}
\usage{
predictable_rejections(ep, alpha = 0.05)
}
\arguments{
\item{ep}{data.frame. Output of eprocess_predictable().}

\item{alpha}{Numeric. Significance level.}
}
\value{
Named list matching the \code{eprocess_rejections()} format
(\code{threshold}, \code{tau_pq}, \code{tau_qp}, \code{reject_pq}, \code{reject_qp}), plus:
\itemize{
\item \code{c_range} — range of \code{c_seq} used.
\item \code{lambda} — \code{lambda} value used.
}
}
\description{
Summarise predictable bounds e-process
}
\examples{
scores1 <- c(0.10, 0.20, 0.15, 0.25)
scores2 <- c(0.05, 0.10, 0.10, 0.20)
c_seq <- rep(1, length(scores1))
ep <- eprocess_predictable(scores1, scores2, c_seq = c_seq)
predictable_rejections(ep, alpha = 0.05)

}
