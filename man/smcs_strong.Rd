% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smcs.R
\name{smcs_strong}
\alias{smcs_strong}
\title{Sequential Model Confidence Set (Strong & Uniformly Weak Null)}
\usage{
smcs_strong(
  scores,
  alpha = 0.05,
  method = c("betting", "mixture"),
  c_param = NULL,
  lambda_param = NULL,
  ...
)
}
\arguments{
\item{scores}{A \eqn{T \times m} matrix of positively-oriented scores.}

\item{alpha}{Numeric in \verb{(0, 1)}. Family-wise significance level. Default is \code{0.05}.}

\item{method}{Character. \code{"betting"} (uses \code{\link[=eprocess_betting]{eprocess_betting()}}, true strong null)
or \code{"mixture"} (uses \code{\link[=eprocess]{eprocess()}}, tests uniformly weak null). Default is \code{"betting"}.}

\item{c_param}{Numeric scalar, \eqn{m \times m} matrix, or \eqn{T \times m \times m} array.
The predictable bound parameter. Required. Time-varying arrays are only allowed
if \code{method = "betting"}.}

\item{lambda_param}{Optional parameter for betting fractions, matching the shape
allowed for \code{c_param}. Only used if \code{method = "betting"}.}

\item{...}{Additional arguments passed to the underlying pairwise e-process function
(e.g., \code{v_opt} and \code{clip_max} for \code{"mixture"}; \code{clip_max} for \code{"betting"}).}
}
\value{
A list containing:
\describe{
\item{\code{E_i_dot}}{A \eqn{T \times m} matrix of unadjusted intersection e-processes.}
\item{\code{E_star}}{A \eqn{T \times m} matrix of closed-testing adjusted e-processes.}
\item{\code{smcs}}{A \eqn{T \times m} logical matrix. \code{TRUE} indicates the model remains
in the SMCS at time \code{t}.}
}
}
\description{
Constructs a Sequential Model Confidence Set (SMCS) evaluating a family-wise
intersection null hypothesis. By maintaining a running intersection over time,
any model excluded from the set is permanently eliminated.
}
\details{
Depending on the \code{method} chosen, this function tests different hypotheses:
\itemize{
\item \strong{\code{method = "betting"}}: Tests the \strong{Strong Null} hypothesis
(conditional step-by-step superiority). Uses a product-form betting martingale.
\item \strong{\code{method = "mixture"}}: Tests the \strong{Uniformly Weak Null}
hypothesis (average superiority over time). Uses an exponential-mixture martingale.
Because strong superiority implies uniform weak superiority, feeding \code{"mixture"}
into this closed-testing machinery yields a valid (though strictly testing the
uniformly weak null) SMCS.
}

The function computes pairwise e-processes between all models, constructs an
intersection e-process for each model, applies a closed-testing multiplicity
adjustment via \code{\link[=vovk_wang_merge]{vovk_wang_merge()}}, and permanently excludes models when their
adjusted e-value exceeds \eqn{1/\alpha}.
}
\examples{
set.seed(1)
# 3 models, 100 time steps. Model 3 is artificially much worse.
scores <- matrix(runif(300, -0.5, 0), nrow = 100, ncol = 3)
scores[, 3] <- scores[, 3] - 0.5
colnames(scores) <- c("M1", "M2", "M3")

# Using the betting method with a global bound c = 2
res <- smcs_strong(scores, alpha = 0.05, method = "betting", c_param = 2)
tail(res$smcs)

}
