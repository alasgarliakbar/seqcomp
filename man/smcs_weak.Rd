% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smcs.R
\name{smcs_weak}
\alias{smcs_weak}
\title{Sequential Model Confidence Set (Weak Null)}
\usage{
smcs_weak(
  scores,
  alpha = 0.05,
  cs_method = c("bernstein", "hoeffding"),
  c_param = NULL,
  ...
)
}
\arguments{
\item{scores}{A \eqn{T \times m} matrix of positively-oriented scores.}

\item{alpha}{Numeric in \verb{(0, 1)}. Family-wise significance level. Default is \code{0.05}.}

\item{cs_method}{Character. \code{"bernstein"} (uses \code{\link[=cs_bernstein]{cs_bernstein()}}) or \code{"hoeffding"}
(uses \code{\link[=cs_hoeffding]{cs_hoeffding()}}).}

\item{c_param}{Numeric scalar or \eqn{m \times m} matrix. The uniform bound
parameter. Must be constant over time.}

\item{...}{Additional arguments passed to the chosen CS function (e.g., \code{v_opt}).}
}
\value{
A list containing:
\describe{
\item{\code{smcs}}{A \eqn{T \times m} logical matrix. \code{TRUE} indicates the model is
in the weakly superior set at time \code{t}.}
\item{\code{alpha_adjusted}}{The Bonferroni-adjusted significance level applied to
each pairwise sequence.}
}
}
\description{
Constructs a Sequential Model Confidence Set (SMCS) evaluating the weak null
hypothesis that a model outperforms all other candidate models on average
over time.
}
\details{
Uses a joint confidence sequence decoupling result: a model \eqn{i} remains in
the SMCS at time \eqn{t} if and only if, for every competitor \eqn{j}, the
pairwise \eqn{(1 - \alpha/(m(m-1)))}-confidence sequence for the average score
difference does not strictly rule out that \eqn{i} is better than \eqn{j}.

Unlike the strong null, this SMCS does not maintain a strict running
intersection; a model's average score can recover over time, allowing it to
dynamically exit and re-enter the confidence set.
}
\examples{
set.seed(2)
scores <- matrix(runif(300, -0.5, 0), nrow = 100, ncol = 3)
scores[, 3] <- scores[, 3] - 0.5
colnames(scores) <- c("M1", "M2", "M3")

res <- smcs_weak(scores, alpha = 0.05, cs_method = "bernstein", c_param = 2)
tail(res$smcs)

}
