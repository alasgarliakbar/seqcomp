% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/winkler.R
\name{winkler_compare}
\alias{winkler_compare}
\title{Full Winkler comparison pipeline (Proposition 4)}
\usage{
winkler_compare(
  p,
  q,
  y,
  alpha = 0.05,
  base_score = log_score,
  v_opt = 10,
  lower_bound = NULL
)
}
\arguments{
\item{p}{Numeric vector in (0,1).}

\item{q}{Numeric vector in (0,1).}

\item{y}{Numeric vector containing only 0 and 1. Binary outcomes.}

\item{alpha}{Numeric in (0,1). Default: 0.05.}

\item{base_score}{Function. Default: log_score.}

\item{v_opt}{Numeric > 0. Default: 10.}

\item{lower_bound}{Numeric or NULL. See winkler_cs().}
}
\value{
Named list with elements:
\itemize{
\item \code{winkler_scores} — raw Winkler score vector.
\item \code{cs} — \code{data.frame} from \code{winkler_cs()}.
\item \code{etest_p_worse} — one-sided e-process testing whether \code{p} is worse than \code{q}.
\item \code{etest_q_worse} — one-sided e-process testing whether \code{q} is worse than \code{p}.
\item \code{rejections} — list of one-sided rejection summaries.
}
}
\description{
Convenience wrapper that computes Winkler scores, one-sided CS, and
e-process in a single call.
}
\examples{
p <- c(0.7, 0.6, 0.8, 0.65)
q <- c(0.5, 0.7, 0.6, 0.55)
y <- c(1, 1, 0, 1)
winkler_compare(p, q, y, alpha = 0.05)

}
