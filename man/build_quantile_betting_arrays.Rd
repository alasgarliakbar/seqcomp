% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smcs.R
\name{build_quantile_betting_arrays}
\alias{build_quantile_betting_arrays}
\title{Build 3D Parameter Arrays for Quantile Betting}
\usage{
build_quantile_betting_arrays(forecasts, scores, tau, eps = 1e-08)
}
\arguments{
\item{forecasts}{A \eqn{T \times m} matrix of raw quantile forecasts.}

\item{scores}{A \eqn{T \times m} matrix of positively-oriented tick-loss scores.}

\item{tau}{Numeric scalar in \verb{(0, 1)}. The quantile level.}

\item{eps}{Numeric. Safeguard against division by zero. Default: \code{1e-8}.}
}
\value{
A list containing two \eqn{T \times m \times m} numeric arrays: \code{c_array}
and \code{lambda_array}.
}
\description{
Pre-computes the dynamic, pair-specific bounding arrays (\code{c_array}) and betting
fractions (\code{lambda_array}) required to evaluate the Sequential Model Confidence
Set for quantile forecasts under the strong null hypothesis.
}
\note{
\strong{Scale Translation:} This function assumes \code{forecasts} and \code{scores} are
evaluated on the raw, linear scale. To replicate the exact log-scale bounds
used in the Arnold et al. (2026) Covid-19 case study, the forecast matrix and
outcomes must be log-transformed prior to passing them to this pipeline.
}
\examples{
set.seed(3)
fcsts <- matrix(runif(150), nrow = 50, ncol = 3)
y <- rbinom(50, 1, 0.5)
scores <- matrix(0, nrow = 50, ncol = 3)
for(i in 1:3) scores[, i] <- tick_loss(fcsts[, i], y, tau = 0.5)

arrays <- build_quantile_betting_arrays(fcsts, scores, tau = 0.5)
dim(arrays$c_array) # 50 x 3 x 3

}
